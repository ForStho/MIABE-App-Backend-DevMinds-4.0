/**
 * Simple ré-export du service MongoConnection depuis core.
 * Permet aux scripts ou modules d'importer depuis 'database' sans connaître core.
 */
export { DatabaseService } from '../core/database/database.service'; // Ajustez le chemin si nécessaire
